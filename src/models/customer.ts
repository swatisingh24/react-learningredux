class Customer {
    constructor(public id: string, public name: string,
        public address: string, public credit: number,
        public status: boolean, public remarks: string, public photourl: string) { }
}

export default Customer;
